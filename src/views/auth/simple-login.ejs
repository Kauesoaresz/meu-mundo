<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Acesso restrito</title>

  <style>
    /* =========================
       RESET
    ========================= */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* =========================
       VARIÁVEIS
    ========================= */
    :root {
      --bg: #0f0f0f;
      --card: #1b1b1b;
      --green: #62d96b;
      --green-dark: #2e7d32;
      --text: #e0e0e0;
      --muted: #9e9e9e;
    }

    /* =========================
       BODY
    ========================= */
    body {
      height: 100vh;
      background: radial-gradient(circle at top, #1a1a1a, #0b0b0b);
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: Arial, Helvetica, sans-serif;
      color: var(--text);
    }

    /* =========================
       LOGIN BOX
    ========================= */
    .login-box {
      width: 340px;
      background: linear-gradient(145deg, #1f1f1f, #141414);
      padding: 32px;
      border-radius: 14px;

      box-shadow:
        0 0 0 2px var(--green-dark),
        0 20px 60px rgba(0,0,0,0.85);
    }

    /* =========================
       TITLE
    ========================= */
    .login-box h2 {
      text-align: center;
      margin-bottom: 24px;
      font-size: 22px;
      letter-spacing: 1px;
      color: var(--green);
    }

    /* =========================
       INPUT
    ========================= */
    .login-box input {
      width: 100%;
      padding: 14px;
      margin-bottom: 18px;

      background: #0e0e0e;
      border: 2px solid #2a2a2a;
      border-radius: 8px;

      color: var(--text);
      font-size: 14px;
    }

    .login-box input::placeholder {
      color: var(--muted);
    }

    .login-box input:focus {
      outline: none;
      border-color: var(--green);
      box-shadow: 0 0 0 2px rgba(98,217,107,0.25);
    }

    /* =========================
       BUTTON
    ========================= */
    .login-box button {
      width: 100%;
      padding: 14px;

      background: linear-gradient(180deg, #7CFC8A, #4CAF50);
      border: none;
      border-radius: 8px;

      font-size: 14px;
      font-weight: bold;
      color: #0b0b0b;
      cursor: pointer;

      transition: all 0.2s ease;
    }

    .login-box button:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 25px rgba(0,0,0,0.7);
    }

    /* =========================
       FOOTER TEXT (DISCRETO)
    ========================= */
    .hint {
      margin-top: 16px;
      text-align: center;
      font-size: 11px;
      color: var(--muted);
      opacity: 0.6;
    }
  </style>
</head>
<body>

  <div class="login-box">
    <h2>Acesso restrito</h2>

    <form method="POST">
      <input
        type="password"
        name="senha"
        placeholder="Senha"
        required
      >
      <button type="submit">Entrar</button>
    </form>

    <div class="hint">
      Área administrativa
    </div>
  </div>

</body>
</html>
